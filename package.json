{
  "name": "splitty",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "cd splitty-frontend && npm run dev",
    "build": "cd splitty-frontend && npm run build",
    "start": "cd splitty-frontend && npm run start",
    "install": "cd splitty-frontend && npm install --legacy-peer-deps",
    "vercel-build": "cd splitty-frontend && npm install --legacy-peer-deps && npm run build"
  },
  "dependencies": {
    "next": "14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
